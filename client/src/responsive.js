const size = {
    s: '480px',
    m: '768px',
    l: '1024px',
    xl: '1200px'
}
const device = {
    s: `(min-width: ${size.s})`,
    m: `(min-width: ${size.m})`,
    l: `(min-width: ${size.l})`,
    xl: `(min-width: ${size.xl})`
}

export default {size, device}

